Drop an optional `lighthouse.mp4` here.

If present, the final game uses it as the hidden <video> source feed.
If absent (the default), the app renders a deterministic procedural storm
scene on canvas instead — everything still works with zero assets.
